# Copy this file to .env and fill in local secret values.
# .env is ignored by Git; keep real tokens out of commits.

# Bearer token for protected Confluence Server/Data Center pages.
# Used with: --source-type confluence --auth bearer
DOCHIVE_AUTH_TOKEN=
